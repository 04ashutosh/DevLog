server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/devlog_db
    username: postgres
    password: your_password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect